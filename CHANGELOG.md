# Changelog

本项目的重要变更记录在此文件中。

## 未发布

### Added

- 增加 Apple-only 主线政策、完整 tracked tree 门禁与独立 CI；`main` 只接收 Apple 应用及其网站、文档、测试、依赖和构建工具，独立 Windows 客户端长期保留在 Windows 分支。
- 增加 macOS 工作区尺寸分级、统一编辑弹窗骨架及独立 `.macqa` 隔离验收入口，覆盖仪表盘、主机、终端、监控、SFTP、RDP、设置和资源空状态。

### Changed

- macOS 工作台继续使用原生非玻璃外观，并统一窄窗口工具栏、筛选入口、页头、卡片密度、终端标签栏、底部操作栏和检查器选择方式。
- SFTP 完整页与检查器采用各自的操作密度；远程编辑器将搜索、编码、重新载入、导出与另存为收进原生工具栏及溢出菜单。

### Fixed

- 修复窄窗口刷新入口不可达、检查器重复导航、终端关闭按钮长期占位，以及 SFTP/编辑器在紧凑空间中操作拥挤的问题。
- 修复编辑已导入密钥或只修改备注时可能清除 Keychain 私钥口令的问题；只有明确替换或删除才修改已有秘密。

### Verification

- Apple-only 门禁夹具 17/17 通过，开发工作区与发布诊断夹具 24/24 通过；覆盖合法 Apple RDP/FreeRDP 内容、Windows 分支来源及大小写变体、Windows 产品目录、C#/WinUI 工程、Windows 文档/工作流、未知根目录和非 Apple 平台声明。
- 严格并发构建通过且第一方告警为 0；macOS 全量测试 473 项全部通过。通用 Mac Release、通用 iOS Simulator 和无签名 iOS Device 兼容构建通过，架构分别为 `x86_64 + arm64`、`x86_64 + arm64` 和 `arm64`。
- 隔离 MacQA 真实窗口已检查 900×620 的仪表盘、编辑与导入／批执行／录制弹窗，以及 1920×1080 的 16 面板终端；当前宿主因 XCTest/TCC automation mode 超时，仅标记 UI 测试 `build-for-testing` 通过。
- 1,000 台主机投影基准由 1,525.353 ms 降至 36.751 ms；10,000 项 SFTP 列表交互总计 71.163 ms；大编辑器尾部行号查询由 2.636 s 降至 0.0000363 s。
- 实际 VoiceOver 朗读、实体串口及真实 SSH/VNC/RDP/WebDAV 服务互通仍需现场验收，不在自动化结果中标记通过。

## 1.0.4 - 2026-09-21

### Added

- 增加 macOS RDP 依赖自举：新工作区通过共享 Scheme 构建时会校验或从锁定源码生成通用 XCFramework，无需预先复制二进制产物。
- 增加 `macos-dev.sh` 的诊断、引导、构建、测试和工程漂移检查入口，以及覆盖缓存损坏、并发引导、失败清理和保留策略的隔离测试。
- 增加只读权限的 macOS CI，校验固定工具链、XcodeGen 工程漂移、严格并发、第一方零告警、RDP 原生探针及主分支 Release 兼容构建。
- 增加 [macOS 构建自举与工作区可靠性验收记录](Docs/MAC_BUILD_RELIABILITY_QA.md)，记录冷构建、离线缓存、全量测试和跨平台 Release 构建的实际结果。

### Changed

- 将 macOS 与移动端版本元数据提升至 1.0.4（Build 9）；Swift 语言模式保持 5.9，macOS 应用及测试继续启用完整严格并发检查。
- FreeRDP 构建改用内容寻址共享缓存；最终产物目录仅保存 XCFramework、必需头文件和清单，下载、锁、最近使用顺序和活动工作区租约采用独立目录。
- macOS 开发及发布命令统一使用共享 Scheme；直接使用 `xcodebuild -target` 不再是受支持的入口。
- Windows RDP 证书检查接受自签发叶证书与自签发链，并在工作组/本地账户未填域时用单标签证书 CN 作为 NLA 域。RDP 仍为开发中能力，不作为已完成的 Windows 互操作验收。
- NLA 域名推断改为纯 Swift，使 `ServerDashMobile` 在不链接 FreeRDP 桥的情况下保持可编译。

### Fixed

- 修复删除 `.build`、移动或新建工作区后因缺少 `ServerDashRDP.xcframework` 导致 Xcode 构建失败的问题。
- 修复缓存目录存在但内容损坏、架构不完整或工具链已经变化时仍可能被误判为可用的问题。
- 修复 Scheme 预操作继承其他 Apple 平台部署目标、Bash 3.2 空数组清理以及原生负载嵌入临时构建前缀造成的冷构建失败与不可搬移问题。
- 缓存与底层构建目录增加所有权校验，避免错误的路径覆盖值清理无关文件；活动工作区租约避免并行构建期间裁掉仍在使用的产物。
- 修复仪表盘刷新控件丢失，以及录制排空在回调前未等待待写入完成的问题。
- 修复 hop 扫描测试在分离任务中修改捕获变量触发的第一方严格并发告警。
- 修复工作组 Windows 主机以 IP 连接时，FreeRDP 证书链被误判为损坏，以及空域 NTLMv2 无法通过 NLA 的问题。

### Security

- 直接 SSH 连接固定 `-F none`；临时私钥改为唯一落盘路径并在启动计划中清理。
- 跳板第 2 跳及之后的主机密钥扫描仅使用已授权前缀；缺少 `ssh-keyscan` 时 fail-closed。
- ProxyCommand 拒绝控制字符；Keychain 项使用 `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` 且不可 iCloud 同步。
- LocalCommand 握手使用独立标记；导入路线应用 `SSHConfigRouteImport.endpoint`；哈希改为流式 SHA-256。
- 隧道停止与等待队列在阻塞头节点时不再卡住后续清理。SwiftData V5、同步包和凭据格式保持不变。

### Verification

- GitHub Actions `macOS` 工作流在发布前提交 `4d4d9fc` 上通过：严格并发、第一方零告警、macOS 测试、RDP 原生探针，以及通用 macOS Release 与 iOS Simulator/Device 兼容构建。
- `Scripts/build-release-artifacts.sh 1.0.4` 完成三端 Release 与无签名 iOS Device 兼容构建；元数据均为 1.0.4（Build 9）。macOS 与模拟器为 arm64/x86_64，设备兼容构建为 arm64。四项附件通过 `ServerDash-1.0.4-SHA256SUMS.txt` 回读校验。
- 实际 VoiceOver、实体串口、实体移动设备及真实 SSH/VNC/RDP/WebDAV 服务互通需要现场验收，不标记为通过。RDP 仍为开发中。详见 `Docs/WORKBENCH_UI_QA.md` 和 `Docs/RELEASE_NOTES_1.0.4.md`。

## 1.0.3 - 2026-09-13

### Changed

- 将 macOS 与移动端版本元数据提升至 1.0.3（Build 8）；Swift 语言模式保持 5.9，并为 macOS 应用及测试目标启用完整严格并发检查。
- macOS 退出统一交由 `MacTerminationDelegate` 和单一关闭协调器处理。草稿保存成功后，目录同步、监控、隧道、受管进程、交互会话、文件传输和录制共享 8 秒绝对期限。
- 退出超过 300 毫秒时显示原生无取消进度层；快速退出不闪现，草稿保存错误出现前先关闭进度层。
- 进程与隧道的退出竞争由一次性 continuation 收口，录制排空改用异步通知；关闭阶段不再通过 `DispatchGroup.wait` 阻塞工作线程。
- 日志可观察状态、监控历史和 SwiftData 上下文收紧到主线程边界；Keychain 查询、导入凭据、终端主题、通知回调和 RDP worker 使用明确的值语义、锁或 actor 隔离。
- 可信主机文件位置改为不可变生产存储及可注入测试实例，测试不再共享可变全局路径。

### Fixed

- 修复退出清理入口重复、并发退出可能启动多份关闭流程，以及截止后迟到回调仍能更新状态的问题。
- 修复草稿保存失败前提前关闭连接的问题；现在可以重试保存或取消退出，取消后恢复编辑并保留仍在运行的连接和服务。
- 修复受管进程已退出回调、取消和超时同时发生时 continuation 可能重复恢复的问题，并区分正常完成、强制终止和超时结果。
- 修复终端命令通知观察令牌的释放与主线程切换、RDP 原生 worker 跨并发边界、并发日志写入及可信主机测试隔离问题。
- 修复测试中的 actor 隔离、共享 URLProtocol handler、并发计数器和旧辅助功能 API 编译告警。

### Security

- 关闭报告仅记录固定组件、结果和耗时；后台事件写入先脱敏再进入 OSLog 与界面事件列表，不记录退出时的主机、路径、命令或凭据。
- 临时密钥材料在正常排空或强制停止后清理；SwiftData V5、同步包和凭据格式保持不变。

### Verification

- 最终源码通过干净严格并发 `build-for-testing` 门禁，第一方源码与测试编译告警为 0；第三方告警实例为 SwiftTerm 87、ZIPFoundation 4、FreeRDP/WinPR 42，NIOSSH 与 Citadel 为 0，均只作信息记录。
- 最终 macOS 全量测试执行 411 项，全部通过、零失败；日志为 `/tmp/serverdash-v103-mac-release-final.log`，结果包位于 `.build/concurrency-gate/Logs/Test/`。
- macOS 通用 Release、iPhone/iPad 通用 Simulator Release 与无签名 iOS Device Release 兼容构建均通过。三端版本均为 1.0.3（Build 8）；macOS 与模拟器二进制包含 arm64、x86_64，设备兼容构建为 arm64。
- macOS ad-hoc 签名、构建目录及 DMG 内 App、DMG 完整性、两份模拟器 ZIP 结构与完整性、离线通知及四项 SHA-256 均通过验证。
- 隔离应用夹具重新生成工作台布局与检查器截图；退出进度层另在 900×620、1440×900 与 1920×1080 下完成浅色/深色、模态键盘焦点契约、无取消入口和辅助功能标签验证。
- 实际 VoiceOver、实体串口、实体移动设备及真实 SSH/VNC/RDP/WebDAV 服务互通需要现场验收，不标记为通过。详见 `Docs/WORKBENCH_UI_QA.md` 和 `Docs/RELEASE_NOTES_1.0.3.md`。

## 1.0.2 - 2026-09-13

### Changed

- 服务器公网 IP 与位置查询改为正向、默认关闭的显式选择加入；启用前说明由受管服务器访问 ipinfo.io，关闭后清理应用内位置缓存。
- SSH 安全设置改为如实显示应用专属主机密钥验证始终启用，并直接提供可信主机管理入口。
- SFTP 多选下载在一次受信任连接中提交全部项目；传输状态不再额外建立轮询连接或反复扫描本地目录。
- 应用退出改为异步排空草稿、目录同步、监控、隧道、子进程和录制写入，再清理临时密钥材料。

### Fixed

- 修复旧版反向位置偏好在缺少键值时可能默认启用公网位置请求的问题；升级不会从旧反向键推断同意，并为降级保留关闭状态。
- 修复 SFTP 多选后下载操作只处理首个项目的问题。
- 修复主机表格仅显示笼统故障状态的问题；现在提供短、脱敏的单机错误摘要及原位重试。
- 修复正常退出可能在后台任务和受管子进程尚未收尾时过早完成的问题。

### Verification

- 新增位置选择加入迁移、SFTP 多选下载计划、主机错误脱敏及进程排空回归；发布验证结果见 `Docs/WORKBENCH_UI_QA.md`。
- 本版本的端口、协议、SwiftData V5、同步包、凭据格式及 iPhone/iPad 能力白名单均未改变。

## 1.0.1 - 2026-09-11

### Added

- 增加 macOS 原生主侧栏、层级分组与标签目录、主机网格与原生 Table、多选整理、隐私模式、统一工具栏及九类设置。
- 增加本地登录 Shell、IOKit/termios 串口会话和系统“屏幕共享”VNC 启动；SSH 增加 Keep-Alive、连接/认证超时、会话日志、交互连接前后本地命令及 HTTP-to-SOCKS 隧道。
- 增加紧凑监控/文件检查器、CPU 用户态/内核态/IO 等待、连接/监听端口与文件句柄指标，以及挂载点和网卡过滤。
- 增加 SFTP 原生多标签远程编辑、草稿恢复、版本冲突保护、本地副本显式回传、目录双向比较和可选的本地改动自动上传。
- 增加 SwiftData V5 固定旧 Schema 与增量迁移；新增组织目录、VNC/串口记录、SSH 高级设置、目录同步任务和配置同步映射，同时保留既有 UUID 与凭据引用。
- 增加默认关闭的 AES-GCM WebDAV 配置同步，支持基线比较、删除标记、冲突预览和 ETag 条件写入；同步密钥保存在 Keychain，并可显式导出或导入恢复密钥。
- 终端工作区增加拖拽标签、最多 16 个 SSH 面板、自由分屏/4×4 网格、固定目标批执行及 15 类预设高亮；会话控制器在切页、布局和检查器变化时继续持有连接。
- 增加 macOS 会话录制与 GIF 导出、可选择提供商的 AI 助手，以及仍处于开发验证阶段的内嵌 RDP。
- 增加 XShell、SecureCRT、MobaXterm、FinalShell、XTerminal、PuTTY、ServerDash JSON 和 OpenSSH Config 的预览导入；支持无凭据的多种格式导出，未完成真实客户端验证的格式继续门控。

### Changed

- 主机工具栏入口去重；窄窗口将分组与标签折叠为筛选菜单，宽窗口恢复用户的分组栏偏好。主机卡片、列表行距、状态与操作区采用更紧凑且可读的布局。
- 主机列表使用一次构建的投影集中计算分组层级、计数、过滤和排序；实时状态仍由单台主机视图独立消费。
- SSH、本地与串口共享明确的命令能力；搜索与远程历史/AI 上下文解耦。主题、字体、字号和系统配色变化只更新现有 SwiftTerm 视图，不重启 PTY。
- 本地/串口终端仅在用户选择终端或明确关闭搜索后恢复输入焦点，不再在视图更新时抢占弹窗、搜索、路径输入或中文输入法焦点。
- 文件编辑器缓存正则和行号索引，以可取消、按文档版本提交的任务应用高亮；草稿编码和写盘移出主线程，退出前等待最新版本落盘。
- iPhone/iPad 仅使用共享 V5 模型、会话迁移结构和桌面类型能力白名单；本地、串口、VNC、WebDAV 等桌面入口不会进入移动端连接或监控页。

### Fixed

- 修复 SFTP 上传忽略目标文件名、特殊字符字面路径转义不足及无效递归删除命令；远程保存使用同目录临时文件替换，并在提交前检查内容版本。
- 修复 SFTP 搜索或隐藏文件过滤后仍可操作不可见选择的问题；同目录刷新保留仍存在的选择，目录读取失败可原位重试。
- 修复连接超时显示值和保存值不一致；设置仅接受 5–300 秒整数，并从应用元数据读取版本信息。
- 修复终端切页、主题、布局、检查器和窗口尺寸变化可能影响焦点或会话生命周期的问题；关闭操作现在覆盖全部工作区标签并正确清理控制器。
- 修复中文组合输入、后台保存、高亮刷新或切换文档时重写正文及破坏光标、选择和撤销记录的问题。

### Security

- 本地连接前后命令和目录自动上传默认关闭；同步或导入的本地命令不会自动启用。连接前命令失败会阻止连接，连接后失败会明确报告。
- 配置同步排除密码、私钥、信任记录、历史、录制、监控数据、本机目录授权和串口设备绑定；会话导出继续排除凭据与 SSH 路线秘密。
- 远程编辑在服务端文件变更、删除或权限失败时保留本地草稿，并要求重新载入、另存为或明确覆盖。

### Verification

- macOS 全量测试 394 项全部通过、零失败；macOS Release 构建及 `ServerDashMobile` 通用 iOS Simulator 兼容构建通过。
- 通过隔离内存数据和模拟服务，在真实应用窗口检查 900×620 浅色、1440×900 深色及 1920×1080 的 16 面板/检查器布局；未访问用户数据库或真实主机。
- 1,000 台合成主机基准中，等价列表查询由约 1,446.9 ms 降至约 36.2 ms；1,300,000 个 UTF-16 单元/50,000 行夹具的末尾行号查询和尾部索引维护均降至亚毫秒。数值仅用于本机前后对比。
- iPhone/iPad 本轮只完成共享接口的模拟器兼容构建；发布附件是 Simulator App，不是 IPA、TestFlight 或 App Store 包。
- 实体移动设备、VoiceOver 实际朗读、真实串口拔插、真实 VNC/RDP/SSH 互操作和真实 WebDAV 双设备同步未执行，不标记为通过。详见 `Docs/WORKBENCH_UI_QA.md`。

## 1.0.0 - 2026-09-05

### Changed

- 将 macOS 与移动端版本统一提升至 1.0.0（Build 5），并增加可复现的正式 Release 构建脚本、发布通知、模拟器安装说明和 SHA-256 校验文件。
- 三端浏览器支持跨字段多关键词搜索和监控开关筛选；每次渲染只排序一次，并预读 SwiftData 排序字段，消除 Mac 列表逐行重复排序。
- 移动端重做服务器概览与资源卡片：明确在线 / 需关注 / 待检测 / 暂停状态，增加 iPad 多列布局、辅助功能字号纵排、实时网络速率、过期提示、下拉刷新、异常重试与直接添加入口。
- 移动端统一前台监控所有权，最多 3 路并发、每台去重、失败封顶退避，设置可调整刷新间隔；后台取消、删除与重新排队使用请求标识隔离，旧任务无法覆盖新状态。
- 移动端主机信任等待支持取消；机器删除明确确认并清理监控与会话；服务器列表、资源卡片和连接详情响应隐藏 IP 设置。
- 补齐 iPad 横竖屏方向声明，修复模拟器旋转后界面仍停留在竖屏的问题；已重新构建并在 iPad Air 11-inch (M3) 模拟器确认横屏左右分栏布局。
- 参考 SwiftServer 官方仪表盘与终端文档，新增服务器搜索、分组 / 标签筛选、稳定排序及仅重试失败监控入口；筛选不改变中央监控范围。
- 新增原生终端检查器（`⌘⌥I`），提供资源快照、过期提示和可搜索的代码片段；执行 / 多行插入经过确认，切换或断开会话后禁止向错误目标发送。
- 统一侧栏分组、页面标题、主操作、搜索框、状态标识和面板层次，减轻卡片阴影并补充搜索焦点与悬停反馈。
- 仪表盘采用对齐的资源指标与独立终端入口，服务器卡片支持原生按钮交互；区分等待检测和已知异常，避免未采集时显示“全部正常”。
- 优化机器网格、列表、身份、密钥、代码片段、可信主机与终端会话页面；机器和会话列表统一响应隐藏 IP 设置。
- 服务器详情与 SFTP 工具栏分行布局；代理和端口转发表单保留可读输入宽度，端口不再显示千位分组符。
- SFTP 首次读取失败时显示重新连接入口，不再误报空目录；移除未使用的旧版监控详情布局。
- 修正负载、网络和内存 / Swap 图表的序列分组，避免不同指标被错误连接为锯齿折线。
- 新增统一、跟随系统区域设置的整数与小数显示格式；数量达到千位后显示分组符，例如 `1000` 显示为 `1,000`。
- 仪表盘、服务器监控、进程与线程统计、历史记录、SFTP、事件日志、连接提示和 Markdown 导出统一使用数字分组格式。
- 端口、PID、版本号以及核心/GPU 序号继续按标识值原样显示，避免改变连接配置、搜索和标识语义。

### Verification

- 1.0.0 发布门禁包含 macOS 通用 Release、iPhone Simulator Release、iPad Simulator Release 和无签名 iOS Device Release 编译；GitHub 移动端附件明确标记为模拟器包，不作为 IPA 或 TestFlight 构建分发。
- 2026-09-05 UI / 功能 / 性能优化：iPhone 17 Pro 与 iPad Air 11-inch (M3) 最终各 23/23 项通过，含本地 Citadel 密码 / Ed25519 / PTY 集成；人工检查测试导出的浅色、深色与辅助功能字号资源卡片。macOS 原有 144/144 项通过；新增搜索测试后全量 145 项中 144 项通过，既有进程取消时序用例以 1.071 秒超过 1 秒门限，单独复跑以 0.216 秒通过。实体设备及全流程 UI 自动化未执行；本轮未修改 SSH 依赖。
- UI 与 SwiftServer 体验参考：Debug 构建通过；使用内存数据检查 900 / 1180 点宽度的深浅色界面及终端检查器。新增 6 项筛选 / 命令安全测试全部通过。完整应用测试为 144 项中 143 项通过；既有进程取消时序测试耗时 2.159 秒超过 1 秒断言，定向复跑以 0.219 秒通过；该次资源模型与取消测试共 14 项全部通过。
- 新增整数千分位、负数及指定小数精度的格式化测试。
- 数字格式化相关测试及其余 118 项测试通过；既有进程取消时序测试在完整套件并发运行时超过 1 秒阈值，单独重跑以 0.231 秒通过。

## 0.1.0-test.5 - S11 专业 SSH 路线与隧道（2026-08-25）

### Added

- 增加统一 `ConnectionProvider`、`ConnectionRoute`、逐跳身份/信任/超时和结构化失败阶段，监控、终端、SFTP 与隧道共享同一 route revision。
- 增加 SSH Config 导入报告、ProxyJump、多跳、SOCKS5/HTTP CONNECT、Local/Remote/Dynamic 转发和连接路线管理页。
- 增加 SwiftData V3 与 V2 → V3 迁移，持久化路线和转发规则；代理 Secret 仅保存于 Keychain。
- 增加 19 项 S11 自动化测试及三跳临时 sshd 隔离验证脚本；详细证据见 [S11 实施状态](Docs/S11_IMPLEMENTATION_STATUS.md)。
- 增加 [0.1.0-test.5 发布记录](Docs/RELEASE_NOTES_0.1.0-TEST.5.md)。

### Security and Reliability

- 明确身份不可读时 fail-closed，不回退到 Agent 或其他默认身份；Agent Forwarding 保持关闭。
- 默认仅监听 `127.0.0.1`；Remote Forward 与 `0.0.0.0`/`::` 每次启动前必须确认并写入脱敏风险/复查审计。
- 隧道启动与 Ready 分离，停止使用 TERM → KILL 并复查进程与本地端口，App Quit 会 drain 全部 app-scoped 隧道。

### Verification

- 完整 Debug 测试 117 项通过、0 失败、0 跳过。
- `arm64`/`x86_64` Release 构建通过；三个本机临时 sshd 的独立身份三跳链路通过。
- Xcode 静态分析通过；仅保留仓库内既有 SwiftTerm 的两个未使用变量警告。
- 未连接真实 VPS，真实代理握手、真实 Remote Forward、硬件密钥和多跳业务长稳仍待隔离环境或实机验证。

## 0.1.0-test.4 - S07 监控历史与 Data Gap（2026-08-25）

### Added

- 增加本地监控历史持久化，保存服务器 ID、采集时间、Collector 及版本、数据质量、源数据年龄和九类基础指标。
- 增加原始样本、聚合记录和 Data Gap 三类 SwiftData 实体，并纳入 VersionedSchema V2 与 V1 → V2 迁移计划。
- 增加 24 小时、7 天、30 天和自定义范围历史页；查询会按显示宽度选择原始值、1 分钟或 15 分钟分辨率。
- 增加超时、不可达、认证失败、主机指纹变化、Mac 睡眠、本机断网、Collector 停止、不支持和未知缺口分类。
- 增加 min/max/avg/last/sampleCount 聚合、按像素降采样、自动保留期清理和 512 MiB 默认磁盘配额。
- 增加可注入监控 Clock 及覆盖退避、jitter、睡眠、断网、恢复、聚合、配额和 10～50 台服务器规模语义的测试。
- 增加 [0.1.0-test.4 发布记录](Docs/RELEASE_NOTES_0.1.0-TEST.4.md)。

### Changed

- `MonitoringCoordinator` 在唯一采集路径记录成功快照和失败原因，并把睡眠、断网、恢复及应用停止接入 Data Gap 生命周期。
- 历史折线按缺口拆分并留白，不再用缺口前的旧值连接出伪连续曲线。
- 持久化存储固定到应用 `Application Support/ServerDash/Data` 目录，并继续提供数据库打开失败时的重试、备份和重建入口。
- 主机信任键改为同时包含主机与端口；缺失身份、密钥或凭据时阻止不安全的 Agent/legacy 回退。
- `CFBundleVersion` 从 2 单调递增到 3；DMG 构建脚本同时生成不包含本机路径的 SHA-256 校验文件。

### Security and Privacy

- 监控历史只保存数值指标和不含主机信息的 Collector 标识，不保存密码、私钥、终端正文、命令、未脱敏主机或敏感路径。
- 主机指纹变化继续阻止连接；相同指纹出现在另一主机时仍需独立信任，不复用旧信任决定。
- 本次包仍为 ad-hoc 签名、未经公证的少量已知测试人员预览版，不包含正式 EdDSA 私钥或更新包。

### Verification

- 完整自动化测试 98 项通过、0 失败。
- Debug 测试构建和 Universal Release 构建通过。
- 1、10、50 台服务器的可重复 fixture、10% 超时场景、V1 → V2 迁移，以及 24 小时/30 天像素有界查询通过。
- Release App ad-hoc 签名、`arm64`/`x86_64` 双架构、DMG 文件系统和 SHA-256 校验通过。
- 真实多服务器长稳、睡眠/切网、Instruments、另一台 Mac 首次启动和真实 SSH/SFTP 仍待实机验证。

## 0.1.0-test.3 - S00 决策基线（2026-08-25）

### Added

- 增加四份 Accepted ADR，冻结内部测试分发与系统 OpenSSH、单业务窗口与 app-scoped 长任务、监控运行承诺、远端平台与 Docker Provider 决策。
- 增加 1.0 核心范围、兼容目标、隐私默认值和非目标清单，明确本产品不设计付费分层。
- 增加架构决策索引，将 D-01、D-02、D-03、D-06、D-07 映射到后续实施阶段。
- 增加 [0.1.0-test.3 发布记录](Docs/RELEASE_NOTES_0.1.0-TEST.3.md)。

### Changed

- 中英文 README 改为与内部测试路线一致：本地 ad-hoc 签名、本人和少量已知测试人员手动分发，不依赖 Developer ID、公证或 Mac App Store。
- 首要远端平台收敛为 Ubuntu LTS 和 Debian Stable；AlmaLinux、Rocky Linux 明确为 capability-gated 兼容目标。
- 位置查询产品约束冻结为新用户默认关闭、首次请求前显式选择加入；当前代码接线仍作为发布前待办公开记录。
- `CFBundleVersion` 从 1 单调递增到 2，便于区分本次内部测试包。

### Security and Compatibility

- 保持系统 OpenSSH、应用专属 `known_hosts` 和 `StrictHostKeyChecking=yes`；没有新增宽松回退。
- 没有修改 SwiftData 数据、Keychain 凭据、真实 SSH 配置或 Sparkle 私钥。
- Accepted ADR 只代表决策冻结，不代表 S01～S06 的代码和实机验收已经完成。

### Verification

- Debug 和 Universal Release 构建通过。
- 完整自动化测试 77 项通过、0 失败。
- Release App 的 ad-hoc 签名、双架构和 DMG 文件系统校验通过。
- 真实 SSH/SFTP、其他测试 Mac 首次启动和长时间运行仍为待实机验证。

## 0.1.0-test.2 - P0 性能与流程加固（2026-08-24）

### Added

- 增加固定名称、无运行时元数据的 OS Signpost，覆盖启动、数据库、监控、主机信任、子进程、仪表盘、终端和 SFTP 性能边界。
- 增加子进程输出上限、超时、取消、进程组清理、按服务器隔离及 FIFO 等待队列的回归测试。
- 增加可信主机稳态零扫描、强制复核、FIFO 信任请求和精确恢复原操作的回归测试。
- 增加中央监控调度器的并发、优先级、去重、退避、低电量、睡眠和网络暂停测试。
- 增加每服务器 `ServerRuntimeState`、原子结果发布和增量舰队汇总模型；新增 100 个 RuntimeState 更新隔离测试。
- 增加首次监控快照的明确等待界面，空快照不再显示为真实的 0% 指标。
- 增加 [P0 实施与验证状态](Docs/P0_IMPLEMENTATION_STATUS.md)，逐批记录需求映射、自动化结果和待实机项目。

### Changed

- 子进程 stdout/stderr 改为 32 KiB 分块读取，达到上限立即终止；取消使用进程组 TERM → KILL 升级并保留结构化终止原因。
- 可信服务器的普通连接直接使用应用 `known_hosts`，只在首次使用、显式复核或主机密钥错误后执行 `ssh-keyscan`。
- 监控刷新收敛为唯一 `MonitoringCoordinator`：每服务器去重，按手动、选中、可见、后台和重试排序，动态补位并限制启动速率。
- 低电量模式下自动监控并发降为 2 且间隔加倍；睡眠或断网会取消监控任务，所有暂停原因解除后再错峰恢复。
- 仪表盘卡片、机器列表和服务器详情改为观察各自 RuntimeState；舰队统计由独立 Store 增量维护，不再扫描全部服务器。
- 监控失败只更新错误状态并保留最后成功的快照和历史，避免详情页跳空。

### Security and Privacy

- SSH 和 SFTP 继续强制 `StrictHostKeyChecking=yes`，使用应用专属 `known_hosts`，不引入宽松回退。
- 性能标记、事件日志和本轮涉及的诊断路径不记录 IP、域名、用户名、文件路径、命令、指纹或凭据。
- 主机密钥变化仍会阻止连接，只有对应信任请求被明确接受后才恢复原操作。

### Verification

- 修改前基线：49 项测试通过。
- 性能测量、子进程、信任队列和中央调度器四个批次依次达到 51、60、66、72 项全量测试通过。
- RuntimeState 批次完成后，Universal Release 构建成功，完整测试为 77 项通过、0 失败、0 跳过。
- Ad-hoc 测试 DMG 已通过 `codesign --verify`、`hdiutil verify` 和 `lipo` 双架构检查；此构建未使用 Developer ID 签名且未公证。
- 100 台服务器 SwiftUI Body Updates、应用 RSS、Energy Log、真实 SSH/SFTP 连接数和终端交互延迟仍需 Instruments/实机验证。

## 0.1.0 - 2026-08-24

### Added

- 统一 SSH/SFTP 子进程连接引擎，支持结构化状态、超时、取消、输出限制和并发控制。
- 常驻多标签终端会话，支持跨服务器切换、连接状态展示、断线原因和重新连接。
- 应用专属 `known_hosts`、首次主机指纹确认、密钥变化对比及可信主机管理。
- 离线保存、独立 SSH 测试、服务器能力探测和验证状态。
- 私钥口令、私钥优先密码回退、外部密钥书签及 Keychain 私钥导入。
- 版本化 SwiftData Schema、数据库打开失败恢复、可信主机和终端会话历史实体。
- OSLog 分类、服务器事件日志和可复制的脱敏 SSH Diagnostics。
- Linux 监控能力探测、过期状态、连接延迟、无重叠刷新和可彻底关闭的位置采集。
- 完整 SFTP 浏览器：文件夹传输、进度取消、冲突策略、移动、递归删除和默认路径。
- 来源感知的服务器详情返回流程，保留仪表盘/机器列表滚动位置和搜索上下文。
- CPU、内存、网络、存储、进程、GPU 和 Docker 的统一监控卡片与详情布局。
- 20 套本地终端主题、系统等宽字体发现、行高、字间距、光标、滚动条和 Bell 设置。
- 全局终端默认外观及会话级即时覆盖，支持 `⌘+`、`⌘-` 和 `⌘0`。
- 仓库内固定的 SwiftTerm 1.11.2 本地分支及终端主题许可证说明。
- 无需 Apple Developer 账号的 Universal Ad-hoc 测试 DMG 构建脚本。

### Changed

- README 采用英文默认页与简体中文独立页面，并提供双向语言切换。
- 服务器保存不再依赖监控采集成功；终端和 SFTP 可在监控不兼容时独立使用。
- 监控卡片详情、事件日志和诊断预览统一使用可点击遮罩关闭的 macOS 风格浮层。
- 终端会话栏改为单行 Apple 风格标签，并与当前终端主题背景衔接。
- 刷新入口收敛为单一“刷新全部”按钮。
- 设置页拆分为通用设置和终端主题设置。

### Fixed

- 修复切换终端标签或页面时 SSH 进程被意外终止。
- 修复 OpenSSH 无法读取包含空格的应用 `known_hosts` 路径。
- 修复主机密钥变化时新旧指纹显示未知或错误判定为相同密钥变化。
- 修复关闭位置采集后远端脚本仍可能访问 `ipinfo.io`。
- 修复数据库打开失败直接触发 `fatalError`。
- 修复基础监控采集重叠和页面切换导致的取消问题。
